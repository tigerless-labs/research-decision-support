# skill v2 — 按 output/architecture.md 落地文件结构

设计真身：工作区 `docs/design-harness/output/`（architecture.md 两棵树 +
system.md + modules/）。本计划把 skill 包一棵树实现进
`plugins/design-harness/skills/design-harness/`。
v1 已整体入 `archive/skill-v1/`，只取材不改动。

## 范围

- 做：skill 包全部文件（SKILL.md、references/、templates/、scripts/、canvases/、
  styles/）、repo 级 tests/、docs/index.md、docs/testing.md、docs/TODO.md、
  plugin.json 与 marketplace.json 描述。
- 不做（入 TODO）：README 重写、examples/autoharness 迁 v2 schema、CI 工作流、
  单画布三团模板实现（本次只入设计规格）。

## 单元（每单元测试先行）

1. **文档与元数据**：docs/index.md 命令行改 v2、docs/testing.md 测试地图重写、
   TODO 勾销与新增、两份 plugin 描述改 v2 定位（三层 + 画板 + 人拍板）。
2. **协议与模板（纯文档）**：SKILL.md（操作规程：跑腿清单、二态、双向同步、
   就地教学、logs 纪律、tag/引用规则、形态与模板选择）、references/protocol.md、
   references/output-forms/system-design.md、templates/{source-card,idea-card,
   target,logs}.md、canvases/single-canvas/spec.md。
3. **工作区工具（TDD）**：tests/test_workspace_tools.py →
   scripts/{init_workspace,check_workspace,check_doc_links}.py。
   校验契约：ideas（含 archive/）必填 id/type；全层 tags 至多一个；idea 卡间
   引用不得成环；sources 卡只查 tags 约束；悬链全树报错。
4. **画板生成器（TDD）**：tests/test_build_canvas.py →
   scripts/build_canvas.py + canvases/tabbed-gallery/template.html。
   规则（收卡、frontmatter tag、排除 archive、payload 注入）在生成器；
   形（HTML/CSS/交互）在模板。红队：卡文注入 `</script>`/事件属性必须成文本。
   输出只写指定目录（默认拒绝写进工作区）。
5. **风格包搬运**：从 worktree-style-pack 分支取 styles/（4 款 + selection-index）
   + scripts/check_style_pack.py + tests/test_style_pack.py，SKILL.md 接线。
6. **端到端验证**：init 临时工作区幂等；三校验器对真实工作区全绿；
   build_canvas 对真实工作区出单文件 HTML，headless 截图核对三 tab。

## 验收

- `python3 -m pytest tests -q` 全绿；校验器对 `docs/design-harness` 全绿。
- 生成的 HTML 与现行 artifact 同构（三 tab、tag 筹码、系统图）。
- 投影不入库：build 产物只落临时目录。

## 追加单元（同 PR）：board 自由面

7. **board 目录（TDD）**：第四工作区目录，一文一板无 schema。校验契约：board 无必填
   字段；单 tag 规则照常；**三层不得引用 board**（最下游）；环检查覆盖 board。
   init 骨架、collect 收板文、画板加 board tab（doc 视图复用系统图机制）、SKILL.md
   与 protocol.md 补规则。
