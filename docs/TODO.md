# TODO

- [ ] `tools/check_research_loom.py`：SKILL.md 已引用的 frontmatter 校验器尚未实现（必填字段、status 枚举）。
- [ ] map 页节点点击跳转到底层卡片文件（节点 id 已含路径，加 href 即可）。
- [ ] 仓库尚无 remote/CI：配置 GitHub remote 后补 PR 流程与 CI（pytest + 链接校验）。
- [ ] 发布时用 Pages CI 从 examples/autoharness 现场生成 demo 站（HTML 不入库，README 挂链接）。
- [ ] read 页导出补丁的自动落盘工具（现靠 agent 手动写回）。
